# Backend-only env for running the API on the host (outside compose).
# When running via docker compose, the root .env drives everything instead.
DATABASE_URL=postgresql+psycopg://app:app_dev_password@localhost:5432/app
API_ENV=development
CORS_ORIGINS=http://localhost:5173
